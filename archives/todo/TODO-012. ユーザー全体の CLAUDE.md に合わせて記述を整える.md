# TODO-012. ユーザー全体の CLAUDE.md に合わせて記述を整える

## きっかけ

ユーザー全体の `CLAUDE.md` が更新されたので、その内容とこのリポジトリの
記述を突き合わせた。差分は 4 点あった。

手順そのもの（TODO の立て方、コミットを 2 回に分ける、サブエージェントの
編成と定義の残し方）はユーザー全体側に書いてあるので、**プロジェクト側では
繰り返さない**方針にした。プロジェクトの `CLAUDE.md` には、このリポジトリ
固有の事実と決まりだけを残す。

## やったこと

1. **造語「契約」を置き換えた。** ユーザー全体の `CLAUDE.md` が名指しで
   禁止している語だった。「モジュール間で受け渡す唯一の形式」にした。
   - `CLAUDE.md` の「アーキテクチャ」
   - `docs/REFERENCE.md` の「3.1 `ParsedMidi`」

   `archives/` にはこの語は出てこなかったので、直したのはこの 2 か所だけ。

2. **`CLAUDE.md` に「タスク管理」の節を足した。** 本文が `（TODO-007）`
   `（TODO-009）` と番号で参照しているのに、`TODO.md` と
   `archives/todo/` が何なのかがどこにも書かれていなかった
   （`docs/REFERENCE.md` に一行あるだけだった）。位置づけと、番号が
   archives のファイルを指すことだけを書き、進め方はユーザー全体の
   `CLAUDE.md` に従うと明記した。

3. **`git` 節にタグの扱いを足した。** push については書いてあったが、
   「自分からはタグを打たない」は `TODO.md` の補足にしかなかった。
   バージョンは hatch-vcs でタグから決まる＝タグを打つことがリリースの
   判断そのものなので、`git` 節に置いた。

4. **`TODO.md` の「補足」節をなくした。** 骨格に無い節だった。中身は
   2 つに分けた。
   - push / タグの決まり → 3 で `CLAUDE.md` の `git` 節へ
   - `0.1.0` / `0.2.0` を付けた経緯 → `TODO-007` の archives ファイルに
     「タグ」節を作って移した（`0.2.0` が TODO-003〜007 の分なので）

## やらないと決めたこと

- **`TODO.md` の骨格と `archives/todo/` の構成は触らない。** 「きっかけ /
  やったこと / テスト」は 11 件すべて揃っていた（TODO-007・008 に
  「決めたこと」が挟まるが、逸脱ではない）
- **サブエージェント用のディレクトリは先に作らない。**
  `archives/agents/` も `.claude/agents/` も、該当する項目がまだ無い。
  必要になったときに作る
- **「コミットを 2 回に分ける」はプロジェクト側に書かない。**
  ユーザー全体の `CLAUDE.md` にあり、履歴も従っている。重複させない

## テスト

文書だけの変更で、`src/` と `tests/` は触っていないので、pytest / ruff /
mypy / basedpyright は動かしていない。

- `grep` で、`archives/` を除いた `*.md` に「契約」が残っていないことを
  確認した（`archives/` 側も 0 件）
- `TODO.md` の目次から `archives/todo/` へのリンクが全件つながることを
  確認した
